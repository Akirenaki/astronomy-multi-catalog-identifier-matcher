GEMINI_API_KEY=key-from-google-ai-studio
DATABASE_URL=database-connection-string
SESSION_SECRET_KEY=long-random-string
USER_SECRET_ENCRYPTION_KEY=fernet-key
FORCE_HTTPS_COOKIES=true # Set to true for any deployment reachable over HTTPS (marks the session cookie Secure). Only set to false for local http://localhost development.